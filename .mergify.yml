pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge
        
  - name: Ask to resolve conflict
    conditions:
      - conflict
    actions:
      comments:
        message: This pull request is now conflicts. 

